<h1><%= t "events.index.events" %></h1>

<div class="mt-3 mb-3">
  <%= link_to t("events.index.create"), new_event_path, class: "btn btn-success" %>
</div>

<div class="row mb-3">
  <%= render partial: "events/event", collection: @events %>
</div>
